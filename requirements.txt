# --- Core Data Science ---
numpy
pandas
scipy
matplotlib
seaborn
tqdm
joblib
scikit-learn
scikit-image

# --- Deep Learning (PyTorch) ---
# On Mac, pip will automatically grab the MPS-enabled version if available
torch
torchvision

# --- Computer Vision ---
# We use headless to be safe for Streamlit/Servers. 
# If you run debug scripts locally, plots will show inline or save to disk.
opencv-python-headless

# --- Segmentation Models ---
# Standard SAM (Used in your V3 pipeline)
git+https://github.com/facebookresearch/segment-anything.git

# FastSAM (For real-time app optimization later)
ultralytics

# --- Web App Framework ---
streamlit
watchdog  # Makes Streamlit auto-reload faster on Mac